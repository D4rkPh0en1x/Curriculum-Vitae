{%  extends 'base.html.twig' %}



{% block body %}
<div class="datacontainer bg-secondary">
	<div class="card">
            {% for row in evalcategories %}
                        <h2>{{ row.label }}</h2>
            {% endfor %}
	</div>
</div>

{% endblock %}